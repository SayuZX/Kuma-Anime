# Kuma Anime — environment configuration
#
# The app now talks directly to free public APIs (Jikan, AniList, Kitsu,
# MangaDex) and needs NO secret keys or private proxy URLs to run.
# Copy this file to `.env.local` to override defaults.

# Logging verbosity for the built-in logger: debug | info | warn | error | silent
# Defaults to "debug" in development and "warn" in production.
NEXT_PUBLIC_LOG_LEVEL=warn
